# Imprime por separado los elementos de una lista.
def run():
    list = ["Pizza", "Tacos", "Tortas", "Hamburgesas"]
    for i in list:
        print(f'Hay que ir a comer {i}.')

if __name__ == "__main__":
    run()